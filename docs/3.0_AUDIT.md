# JP Costco Shop 3.0 — 第一階段完整審查報告（2026-09-07）

> 依「JP Costco Shop 3.0 — AI Autonomous Cross-border Commerce System」需求，對 `main`（8505329）完整審查後產出。
> 執行原則：不覆蓋 2.0 既有功能，3.0 以並行模組接入（見 `~/JP-Costco-3.0-SPEC/SPEC.md`）。

## 已完成（應保留）
| 模組 | 位置 | 狀態 |
|---|---|---|
| 購物網站（商品頁/購物車/結帳/報關表單） | `app/costco/*`、`lib/validation.ts` | ✅ 完整，Zod 驗證、身分證遮罩 |
| 訂單系統（Supabase） | `lib/orders.ts`、`supabase/schema.sql` | ✅ 保存下單當時名稱與價格 |
| 後台（登入/審核/發布/訂單/批次/匯出） | `app/admin/*`、`lib/auth.ts` | ✅ HMAC cookie、Route Handler 模式 |
| 每日搜尋 cron | `app/api/cron/run-search`、`render.yaml` | ✅ 每日 08:00，失敗保留上一期 |
| Costco 官網抓取 | `lib/costco-fetch.ts`（DOM）、`lib/costco-api.ts`（官方 REST API） | ✅ 含 Hot Buy/排除規則 |
| 價格比較 | `lib/price-compare.js`（Yahoo JP / Amazon JP） | ✅ |
| 排名引擎 | `lib/ranking.ts`（0–100 分、權重、shouldExclude） | ✅ 有測試 |
| 現場照片管線（照片收集） | `lib/google-drive-sync.ts`、`lib/media-processing.ts` | ✅ Drive 全分頁→Queue→HEIC/MOV 轉檔→私有 Storage |
| Graph 相關表 | `supabase/schema.sql` 內 photo queue / media assets / vision candidates / pairings / weekly_store_deals | ✅ 已建表，RLS 私有 |
| 測試 | `tests/*`（5 檔） | ✅ vitest |
| 部署 | Render Web + Cron（`render.yaml`） | ✅ |

## 部分完成
| 模組 | 現況 | 3.0 補完 |
|---|---|---|
| Vision 辨識 | 只有表（`costco_vision_candidates`）與狀態欄位；`process` route 只做轉檔，無模型呼叫 | Vision Agent（僅候選資料，人工 VERIFIED 後才發布） |
| 商品/價牌配對 | 只有表（`costco_photo_pairings`），無實作 | 依 Item Number/JAN/視覺相似配對 |
| LINE 通知 | Phase 1 就緒；`notifications` 表有寫入、無實際發送 | Agent 6 客戶觸點 |
| README / AGENTS.md | 仍寫「SQLite（node:sqlite）為主」 | 資料層已遷移 Supabase（f71147e），文件需更新 |

## 尚未完成（＝3.0 核心，Phase 9–14）
- Product Intelligence Graph 表（`~/JP-Costco-3.0-SPEC/graph-schema.sql` 未落地）
- Agent 1 商品雷達：競業代購影片訊號（YouTube/IG/TikTok）、Reseller Promotion Signal
- Agent 2 購買意圖（`intent_signal` 規則引擎）
- Agent 3 適合度（現 `estimate()` 用常數 taiwanDemand=0.5/logisticsFit=0.6，非正式評分）
- Agent 4 AI 採購主管（`score_snapshot` 決策與理由）
- Agent 5 自動文案/上架（目前全人工 `PublishForm`）
- Agent 6 訂單擴充（運費閘門/採購清單/客服）
- Ground Truth 匯入（`~/costco-analysis/` 直播逐字稿＋商品清單＋截圖）

## 應修正
1. ~~**雙軌資料層不一致**：`scripts/run-search.js` 仍寫 SQLite，而 cron 實際用 `lib/search.ts`（Supabase）——手動執行會寫進不同的資料庫。~~ ✅ **Phase 14 已修**：改為 `scripts/run-cron.mjs`（`npm run search:run` / `agents:run` / `publish:run`），手動與 cron 共用同一條 Supabase API route。
2. ~~README/AGENTS.md 技術描述過時（SQLite → Supabase）。~~ ✅ **Phase 14 已修**。
3. ~~`lib/db.ts` 的 photo queue/weekly_deals 表定義與 `supabase/schema.sql` 重複維護，易漂移。~~ ✅ **Phase 14 已移除 `lib/db.ts`**（全專案無人 import）。

## 可刪除的舊架構（SQLite 路徑，確認 Render 已無依賴後）
✅ **Phase 14 已執行**（Render 的 web／cron 皆為 `curl` API route，無 SQLite 依賴）：
`lib/db.ts`、`lib/schema.sql`、`scripts/run-search.js`、`scripts/scrape-top50.js`、`scripts/scrape-categories.js`、`scripts/upgrade-products.js`、`scripts/seed.js`、`scripts/init-db.js`、`scripts/seed-published.js`、`@types/better-sqlite3`
保留：`scripts/published-snapshot.json`（由 `scripts/seed-supabase.js` 還原用）、`lib/price-compare.js`（改由 `scripts/sync-comparison-prices.js` 寫入 Supabase）。

## Astra 最值得介入的部分（其餘下沉至規則引擎/Sol）
1. 跨來源商品實體比對（`product_entity` 合併判斷，每天批次一次）
2. 消費者模糊留言的購買意圖理解
3. 競業直播/影片內容綜合理解
4. 商品是否適合台灣代購的深度判斷
5. AI 採購主管最終決策（唯一常駐）

## 實作順序（本分支）
Phase 9 Graph migration → Agent 1–3 規則引擎（含測試）→ Agent 4 決策（Astra hook + 規則 fallback）→ Agent 5 草稿 → Agent 6 訂單擴充 → Dashboard 漏斗 → 直播資料匯入腳本 → 測試/建置/文件。